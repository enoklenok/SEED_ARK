// * components
import { GlobalNavigationBar } from '@components/header';

const Header = () => {
  return (
    <header className="h-20 lg:px-60 md:px-40 px-20">
      <GlobalNavigationBar />
    </header>
  );
};

export default Header;
